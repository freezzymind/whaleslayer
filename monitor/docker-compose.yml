version: '3.8'

services:

  postgres_exporter:
    image: quay.io/prometheuscommunity/postgres-exporter
    container_name: postgres_exporter
    restart: always
    environment:
      DATA_SOURCE_NAME: "postgresql://reader:${READER_PASSWORD}@${POSTGRES_DB}:5432/${POSTGRES_DB}?sslmode=disable"
    networks:
      - whaleslayer_local
  
  node_exporter:
    image: prom/node-exporter
    container_name: node_exporter
    restart: always
    pid: host
    network_mode: host
    volumes:
      - /proc:/host/proc:ro
      - /sys:/host/sys:ro
    command:
      - '--path.procfs=/host/proc'
      - '--path.sysfs=/host/sys'
      - '--web.listen-address=127.0.0.1:9100'

  prometheus:
    image: prom/prometheus
    container_name: prometheus
    restart: always
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
    ports:
      - "0.0.0.0:9090:9090"
    networks:
      - whaleslayer_local

  grafana:
    image: grafana/grafana
    container_name: grafana
    restart: always
    ports:
      - "0.0.0.0:3000:3000"
    environment:
      - GF_USER=${GF_USER}
      - GF_PASSWORD=${GF_PASSWORD}
    env_file:
      - /root/projects/whaleslayer/.env
    volumes:
      - ./grafana-data:/var/lib/grafana
    networks:
      - whaleslayer_local

  pgadmin:
    image: dpage/pgadmin4
    container_name: pgadmin
    restart: always
    ports:
    - "0.0.0.0:8080:80"
    environment:
    - PGADMIN_DEFAULT_EMAIL=${PGADMIN_EMAIL}
    - PGADMIN_DEFAULT_PASSWORD=${PGADMIN_PASSWORD}
    env_file:
    - /root/projects/whaleslayer/.env
    volumes:
    - ./pgadmin_data:/var/lib/pgadmin
    networks:
    - whaleslayer_local

  
networks:
  whaleslayer_local:
    external: true